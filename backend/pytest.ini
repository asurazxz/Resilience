[pytest]
; Makes `app.*` importable regardless of the invocation directory, without
; needing a package install or a dependency manifest. Requires pytest >= 7
; (pythonpath ini option). Not a substitute for feature/01-foundation-input's
; eventual dependency manifest - just test-collection configuration.
pythonpath = .
testpaths = tests
