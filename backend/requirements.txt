# Provisional backend manifest.
#
# feature/01-foundation-input owns the canonical manifest. This file exists so
# the Scenario Simulator endpoint can be run and demonstrated; replace it when
# the Workstream 1 version lands.

fastapi==0.141.1
pydantic==2.13.5
uvicorn[standard]==0.52.4
